{
  "name": "servidor-app",
  "version": "1.0.0", 
  "type": "module",
  "scripts": {
    "prestart": "npm install --omit=dev --loglevel=error 2>/dev/null || true",
    "start": "node server.js",
    "build": "echo 'Build completo'",
    "dev": "NODE_ENV=development node server.js",
    "install-silent": "npm install --omit=dev --loglevel=error > /dev/null 2>&1"
  },
  "dependencies": {
    "express": "^4.18.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
