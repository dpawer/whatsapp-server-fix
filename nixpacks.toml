[phases.setup]
nixPkgs = ["nodejs_22"]  # APENAS Node.js via Nix

[phases.setup.apt]
pkgs = [
    "chromium-browser",
    "libnss3",
    "libatk1.0-0", 
    "libatk-bridge2.0-0",
    "libcups2",
    "libgbm1",
    "libasound2t64",
    "libpangocairo-1.0-0", 
    "libxss1",
    "libgtk-3-0",
    "libxshmfence1",
    "libglu1-mesa",
    "fonts-liberation",
    "xdg-utils"
]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build] 
cmds = ["echo 'Build completo'"]

[start]
cmd = "node server.js"
